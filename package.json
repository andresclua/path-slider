{
  "name": "clipslider",
  "version": "0.0.1",
  "description": "clip path slider",
  "scripts": {
    "clean": "rimraf dist",
    "start": "npm run clean && parcel serve ./src/index.html --no-cache",
    "build": "npm run clean && parcel build ./src/index.html --public-url ./ --no-cache",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/andresclua/path-slider.git"
  },
  "author": "andresclua",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.0",
    "parcel": "^2.0.0",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "@andresclua/jsutil": "^2.0.4",
    "swipe-listener": "^1.3.0"
  }
}
